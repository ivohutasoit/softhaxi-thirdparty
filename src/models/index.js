'use strict'

const Cache = require('./cache.model');
const Profile = require('./profile.model');
const User = require('./user.model');

/**
 * @since 1.1.0
 */
module.exports = {
  Cache,
  Profile,
  User
}